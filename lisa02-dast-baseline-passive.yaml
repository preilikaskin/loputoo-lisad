env:
  contexts:
    - name: passive
      urls:
        - http://localhost:3000
      includePaths:
        - http://localhost:3000.*
      excludePaths:
        - ^http://localhost:3000/ftp.*$
        - ^http://localhost:3000/ftp/quarantine.*$
        - ^http://localhost:3000/ftp/incident-support\.kdbx.*$
        - ^http://localhost:3000/encryptionkeys.*$
        - ^https?://cdnjs\.cloudflare\.com.*$
        - ^https?://firefox-settings-attachments\.cdn\.mozilla\.net/.*$
        - ^https?://(?!localhost:3000).*$
        - ^http://localhost:3000/socket\.io/.*$
      authentication:
        method: json
        parameters:
          loginPageUrl: http://localhost:3000/#/login
          loginRequestUrl: http://localhost:3000/rest/user/login
          loginRequestBody: '{"email":"{%username%}","password":"{%password%}"}'
        verification:
          method: response
          loggedInRegex: '"authentication"\s*:\s*\{\s*"token"'
          loggedOutRegex: 'Invalid email or password'
      sessionManagement:
        method: headers
        parameters:
          Authorization: 'Bearer {%json:authentication.token%}'
      users:
        - name: admin-user
          credentials:
            username: admin@juice-sh.op
            password: admin123
  parameters:
    failOnError: true
    progressToStdout: true

jobs:
  - type: spider
    parameters:
      context: passive
      user: admin-user
      url: http://localhost:3000
      maxDuration: 3

  - type: passiveScan-config
    parameters:
      enableTags: false
      maxAlertsPerRule: 20
      scanOnlyInScope: true

  - type: passiveScan-wait
    parameters:
      maxDuration: 5

  - type: outputSummary
    parameters:
      format: Long
      summaryFile: /Users/kaisaliiv/Desktop/juice-shop-test-2026/.zap/reports/passive-summary.json

  - type: report
    parameters:
      template: traditional-html
      reportDir: /Users/kaisaliiv/Desktop/juice-shop-test-2026/.zap/reports
      reportFile: passive-report.html
      reportTitle: Juice Shop ZAP Passive Scan

  - type: report
    parameters:
      template: traditional-json
      reportDir: /Users/kaisaliiv/Desktop/juice-shop-test-2026/.zap/reports
      reportFile: passive-report.json
      reportTitle: Juice Shop ZAP Passive Scan JSON
